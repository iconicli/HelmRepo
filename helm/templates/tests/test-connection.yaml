apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "skeleton-app-2023.fullname" . }}-test-connection"
  labels:
    {{- include "skeleton-app-2023.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "skeleton-app-2023.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
